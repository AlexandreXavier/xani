/// <reference types="node" resolution-mode="require"/>
import sharp from 'sharp';
import { MimeType } from 'file-type';
export type ImageMimeType = MimeType | 'image/svg+xml';
export declare const AllImageFormat: string[];
export type ImageFormat = (typeof AllImageFormat)[number] | undefined;
export type Image = {
    format: ImageFormat;
    data: Buffer;
};
export type ImageOutputOptions = {
    resize?: sharp.ResizeOptions;
    toFormat?: 'webp' | 'avif' | 'png' | 'jpeg' | 'jpeg+progressive' | 'unchanged';
};
export declare function compressImage(data: Buffer, options: ImageOutputOptions): Promise<Image | undefined>;
