import { UrlParser, UrlTransformer } from "../types.js";
export interface DirectusParams {
    fit?: "cover" | "contain" | "inside" | "outside";
    quality?: number;
    withoutEnlargement?: boolean;
    /**
     * For even more advanced control over the file generation, Directus exposes the [full `sharp` API](https://sharp.pixelplumbing.com/api-operation).
     *
     * @example
     *
     * ?transforms=[["blur",45],["resize",{"width":200,"height":200}]]
     */
    transforms?: Array<Array<string | number | boolean | Record<string, unknown>>>;
}
export declare const parse: UrlParser<DirectusParams>;
export declare const transform: UrlTransformer;
