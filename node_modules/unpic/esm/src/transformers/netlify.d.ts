import type { UrlGenerator, UrlParser, UrlTransformer } from "../types.js";
export declare const parse: UrlParser;
export interface NetlifyParams {
    /** If set, use this site as the base for absolute image URLs. Otherwise, generate relative URLs */
    site?: string;
    fit?: string;
    quality?: number;
}
export declare const generate: UrlGenerator<NetlifyParams>;
export declare const transform: UrlTransformer;
