import { CanonicalCdnUrl, ImageCdn } from "./types.js";
export declare function getDelegatedCdn(url: string | URL, cdn: ImageCdn): CanonicalCdnUrl | false;
/**
 * Gets the canonical URL and CDN for a given image URL, recursing into
 * the source image if it is hosted on another CDN.
 */
export declare function getCanonicalCdnForUrl(url: string | URL, defaultCdn?: ImageCdn | false): CanonicalCdnUrl | false;
